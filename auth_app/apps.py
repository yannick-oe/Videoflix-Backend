"""Application configuration for the authentication app."""

from django.apps import AppConfig


class AuthAppConfig(AppConfig):
    """Django application configuration for auth_app."""

    name = "auth_app"
